{
  "mcpServers": {
    "resume-tools": {
      "command": "python3",
      "args": ["${CLAUDE_PROJECT_DIR}/mcp/server.py"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
